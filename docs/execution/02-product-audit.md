# 02 — Product audit (pre-foundation)

## Strengths

- Explainable scoring with eligibility hard gates
- Pipeline + follow-ups + application tasks
- Radar Top 3 + briefing hooks
- Evidence/learning JSON on profile + personal seed
- Demo read-only mode and CI quality gates
- Career docs already under `docs/career/`

## Gaps vs Career OS north star

| Pillar | Status |
|---|---|
| Career tracks as first-class config | Partial (classifier/search families only) |
| Evidence vault | Partial (JSON on profile) |
| Heatmap honesty | Partial (false gaps when evidence unlinked) |
| Today / Focus Guard | Partial (logic on Radar, not dedicated page) |
| Networking CRM | Missing |
| Scoring V2 multittrack + golden set | Missing |
| Application kit / ATS variants | Partial (fields exist; no kit flow) |
| Interview / authorship checks | Missing as product surface |

## Foundation slice (this branch)

Tracks + Evidence tables + heatmap recalc + `/today` WIP — **not** Scoring V2 rewrite.
