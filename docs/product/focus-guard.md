# Focus Guard (Today)

Rota `/today` · API `GET /api/today`.

- Máximo **3** ações diárias (`buildDailyActions`)
- Follow-ups atrasados
- Uma meta de aprendizado de alto ROI
- WIP: preparing ≤5, learning ≤2, portfolio projects ≤1 (ajustável em settings)
- Inbox **Não fazer agora** (`settings.dontDoNow`)

Radar continua existindo; Hoje é a home recomendada na sidebar.
