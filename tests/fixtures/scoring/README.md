# Scoring fixtures

Synthetic jobs for regression tests. Labels are human judgment for a **junior/internship** full-stack + data profile.

Metrics derived from this set are **lab metrics**, not production KPIs.

See `tests/scoring-gates.test.ts` and `tests/engine.test.ts`.
